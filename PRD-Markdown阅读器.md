# Markdown 阅读器 — 产品需求文档（PRD）

> **版本**：v1.0  
> **日期**：2026-08-11  
> **状态**：已确认，进入开发  
> **文档定位**：v1 范围的功能与架构契约，v2 功能仅作展望。

---

## 1. 产品概述

### 1.1 一句话定义
一款 **本地优先、双击即开** 的 Markdown 阅读/编辑桌面应用，提供分屏实时预览、目录导航、搜索、导出等核心能力。

### 1.2 核心价值
- **双击 .md → 应用直接打开**（系统级文件关联，无需"先开软件再选文件"）。
- **阅读 + 轻编辑一体**：左边写、右边看，同步滚动。
- **纯本地、零账户、零联网依赖**：数据完全留在用户机器上。

### 1.3 非目标（v1 明确不做）
- 账户系统 / 多用户 / 云协作
- 后端服务器 / 数据库
- 图片粘贴管理（v2）
- 文件树 / 文档库批量管理（偏离"阅读器"定位）

---

## 2. 目标用户与场景

| 维度 | 描述 |
|------|------|
| 用户 | 纯个人自用的开发者/写作者（用户本人） |
| 主场景 A | 本地有一堆 .md 笔记/文档，希望双击即看，能顺手改两笔存回 |
| 主场景 B | 把一段 Markdown 文本粘贴进来，预览排版并导出 HTML/PDF |
| 主场景 C | 同时打开多篇文档对照阅读（多标签） |

---

## 3. 技术架构

### 3.1 技术栈总览

```
┌──────────────────────────────────────────────────────┐
│  Tauri 2（桌面壳 + .md 文件关联 + 系统 WebView）      │
│  ├── single-instance 插件（二次打开 → 新标签）        │
│  ├── store 插件（最近文件 / 窗口状态 / 主题持久化）   │
│  └── fs 插件（读写本地 .md 文件）                     │
├──────────────────────────────────────────────────────┤
│  Vue 3 + Vite + TypeScript（前端）                    │
│  ├── Pinia（标签列表 / 当前状态 / 脏标记）            │
│  ├── CodeMirror 6（左编辑器 + 内置搜索）              │
│  ├── markdown-it（解析 + GFM + TOC 插件）             │
│  ├── Tailwind CSS（应用外壳布局）                     │
│  ├── github-markdown-css（渲染区排版）                │
│  └── CSS 变量（明暗主题切换）                         │
└──────────────────────────────────────────────────────┘
```

### 3.2 关键技术选型理由

| 层 | 选型 | 理由 |
|----|------|------|
| 桌面壳 | Tauri 2 | 产物 ~5-10MB，启动快；文件关联是内置能力；安全模型好 |
| 前端框架 | Vue 3 + Vite + TS | 响应式适合编辑器数据流；Tauri 官方推荐组合 |
| Markdown 解析 | markdown-it | 插件生态成熟，GFM/TOC/高亮链路直白 |
| 编辑器 | CodeMirror 6 | ~150KB 轻量；内置 `@codemirror/search`；原生 Markdown 模式 |
| 状态管理 | Pinia | 多标签/最近文件/主题跨组件共享 |
| 样式 | Tailwind + github-markdown-css + CSS 变量 | 外壳高效、排版专业、主题切换干净 |

---

## 4. 功能需求（v1 = 13 项）

> 优先级图例：✅ v1 必须 ｜ ⭐ v2 展望

### F1. Markdown 渲染（✅）
- 支持基础语法：标题、段落、列表（有序/无序）、代码块、引用、链接、图片、分隔线。
- 支持 **GFM**：任务列表（`- [ ]`）、删除线（`~~`）、表格增强、自动链接。
- 渲染区使用 github-markdown-css 保证排版专业。

### F2. 代码块语法高亮（✅）
- 常见语言（JS/TS/Python/Rust/Go/HTML/CSS/JSON/Bash 等）高亮。
- 实现：markdown-it + highlight.js（或 lowlight）。

### F3. 分屏实时预览 + 同步滚动（✅）
- 左：CodeMirror 6 编辑器；右：渲染预览区。
- 输入即解析、即渲染（防抖 ~150ms）。
- **同步滚动**：左侧滚动 → 右侧按比例/锚点跟随，反之亦然。

### F4. 目录大纲侧边栏 TOC（✅）
- 自动从文档标题（H1-H3）生成大纲树。
- 点击大纲项 → 预览区平滑滚动到对应标题。
- 标题带锚点 id。

### F5. 明暗主题切换（✅）
- 提供 明 / 暗 两套主题。
- 切换通过根元素 `data-theme` 属性 + CSS 变量瞬时切换。
- 记住上次选择（持久化到 store）。

### F6. 本地文件打开 / 拖拽 + 粘贴文本（✅）
- **打开**：菜单/按钮 → Tauri 文件选择对话框 → 读取 .md。
- **拖拽**：把 .md 文件拖入窗口 → 直接打开为新标签。
- **系统关联**：双击 .md → 应用打开该文件。
- **粘贴文本**：新建空白标签 → 粘贴 Markdown → 预览。

### F7. 保存（✅）
- Ctrl+S 写回**原文件**（Tauri fs 写入）。
- 标签显示 ● 脏标记（有未保存改动时）。
- 未保存关闭 Tab / 关窗口 → 弹窗确认（保存/不保存/取消）。

### F8. 文档内搜索（✅）
- Ctrl+F 唤起搜索栏（CodeMirror 6 内置 `@codemirror/search`）。
- 支持上/下一个、高亮所有匹配。

### F9. 导出 PDF / HTML（✅）
- **导出 HTML**：渲染后 DOM + 内联 CSS → 保存对话框写盘为 .html（WebView2 不支持 Blob 下载，改走 Tauri fs；纯浏览器 dev 回退 Blob）。
- **导出 PDF**：调用 Tauri WebView 打印 → 系统打印对话框 → 另存为 PDF（Ctrl+P）。

### F10. 最近打开文件列表（✅）
- 记录最近打开的文件（绝对路径 + 文件名 + 时间）。
- 上限保留 15 条，去重，最新置顶。
- 存储于 `tauri-plugin-store`（落 AppData JSON）。
- 点击列表项 → 重新打开。

### F11. 多标签页 + 窗口状态记忆（✅）
- 单窗口多标签，每个标签独立文档。
- 标签栏：文件名、脏标记 ●、关闭按钮 ×。
- Alt+←/→ 切换标签。
- 记住上次窗口大小/位置 + 上次主题。

### F12. 数学公式（✅）
- 行内 `$...$`、块级 `$$...$$` 公式。
- 使用 KaTeX 渲染；语法错误显示红色提示，不崩溃。

### F13. Mermaid 图表（✅）
- ` ```mermaid ` 代码块渲染为 SVG 图。
- 语法错误显示错误条 + 保留源码。
- 主题跟随应用明暗切换；mermaid 库按需懒加载。

---

## 5. 交互与快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+S | 保存（写回原文件） |
| Ctrl+F | 文档内搜索 |
| Ctrl+P | 打印 / 导出 PDF |
| Ctrl+B / Ctrl+I | 加粗 / 斜体（编辑态，包裹选区） |
| Alt+← / Alt+→ | 切换到上一个 / 下一个标签 |
| Ctrl+N | 新建空白标签 |
| Ctrl+O | 打开文件 |

---

## 6. 非功能性需求

| 项 | 要求 |
|----|------|
| 性能 | 单个 < 1MB 的 .md 解析+渲染 < 100ms；输入到预览延迟 < 200ms |
| 体积 | 安装包 < 15MB（Tauri 目标） |
| 启动 | 冷启动到可交互 < 2s |
| 平台 | 首发 Windows（后续可扩展 macOS/Linux） |
| 数据安全 | 所有内容本地存储，无任何联网上报 |
| 字符编码 | 统一 UTF-8 读写 |

---

## 7. 项目目录结构（规划）

```
md-reader/
├── src-tauri/              # Tauri/Rust 后端
│   ├── src/main.rs
│   ├── Cargo.toml
│   ├── tauri.conf.json     # 文件关联、窗口、插件配置
│   └── icons/
├── src/                    # Vue 前端
│   ├── App.vue
│   ├── main.ts
│   ├── stores/             # Pinia (tabs, settings, recents)
│   ├── components/
│   │   ├── TabBar.vue      # 标签栏
│   │   ├── Editor.vue      # CodeMirror 编辑器
│   │   ├── Preview.vue     # 渲染预览
│   │   ├── TocSidebar.vue  # 目录大纲
│   │   ├── Toolbar.vue     # 工具栏
│   │   └── ThemeToggle.vue
│   ├── composables/
│   │   ├── useMarkdown.ts  # markdown-it 封装
│   │   ├── useFileIO.ts    # 文件读写
│   │   └── useShortcuts.ts # 快捷键
│   ├── styles/
│   │   ├── theme.css       # CSS 变量主题
│   │   └── markdown.css    # 渲染区排版
│   └── types/
└── package.json
```

---

## 8. v2 展望（不在本次范围）

- 图片粘贴管理 / 本地图片引用渲染
- 文档内全文搜索增强（跨文件）
- 命令面板（Ctrl+Shift+P）
- 更精致的 PDF 导出引擎
- 跨平台（macOS / Linux）

---

## 9. 验收标准（v1）

1. ✅ 安装后，双击任意 .md 文件 → 应用打开并渲染该文件。
2. ✅ 应用已开启时，再双击另一 .md → 在已有窗口新增标签。
3. ✅ 分屏左编辑右预览，输入即时同步，双向滚动跟随。
4. ✅ 目录大纲正确生成，点击可跳转。
5. ✅ 明暗主题可切换并记住。
6. ✅ Ctrl+S 保存写回原文件，脏标记正确显示，关闭拦截生效。
7. ✅ Ctrl+F 搜索可用，高亮匹配。
8. ✅ 导出 HTML 可下载且双击打开排版正确；导出 PDF 走打印对话框。
9. ✅ 最近文件列表可点击重开。
10. ✅ 多标签切换正常，Alt+←/→ 可用。
11. ✅ 文档含 `$` / `$$` 公式 → 正确渲染；公式语法错误显示红色提示且不崩溃。
12. ✅ ` ```mermaid ` 代码块 → 渲染为图；语法错误显示错误条 + 保留源码。
13. ✅ 明暗主题切换 → mermaid 图主题跟随。
14. ✅ 导出的 HTML 中公式与 mermaid 图正确显示。
15. ✅ 不含 mermaid 的文档不加载 mermaid chunk（DevTools 网络面板验证），保障 <15MB 安装包与 <2s 冷启动指标不被侵蚀。
